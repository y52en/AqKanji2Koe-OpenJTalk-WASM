# kanji2koe-openjtalk docs

Cloudflare Pages settings:

- Root directory: repository root
- Build command: `npm install && npm run docs:build`
- Build output directory: `docs-src/dist`

Local development:

```sh
npm install
npm run docs:dev
```
